mutation AddComment(
  $courseInitials: String!
  $content: String!
  $parentId: String
) {
  addComment(
    data: {
      courseInitials: $courseInitials
      content: $content
      parentId: $parentId
    }
  )
}
