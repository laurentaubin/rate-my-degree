query Course($initials: String!, $attribute: String!, $order: String!) {
  course(data: { initials: $initials, sortBy: { attribute: $attribute, order: $order } }) {
    initials
    title
    description
    professor
    comments {
      id
      isSubComment
    }
  }
}
